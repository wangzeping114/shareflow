# ============================================================
# ShareFlow 生产环境变量模板
# 复制此文件为 .env，填写真实值后方可启动
# 警告：.env 文件包含敏感信息，禁止提交到 Git 仓库
# 生成强密码命令：openssl rand -base64 32
# ============================================================

# ------------------------------------------------------------
# PostgreSQL 数据库
# ------------------------------------------------------------
POSTGRES_USER=shareflow
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB_OVERSEAS=shareflow_overseas
POSTGRES_DB_DOMESTIC=shareflow_domestic

# ------------------------------------------------------------
# Redis 缓存
# ------------------------------------------------------------
REDIS_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# ------------------------------------------------------------
# MinIO 对象存储（合同文件存储）
# ------------------------------------------------------------
MINIO_ROOT_USER=shareflow-admin
MINIO_ROOT_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# ------------------------------------------------------------
# JWT 认证
# 至少 32 位随机字符串，建议 64 位
# ------------------------------------------------------------
JWT_SECRET=CHANGE_ME_64_CHAR_RANDOM_SECRET_KEY_HERE_AT_LEAST_32_CHARS
